# Avoid unexpected behavior
export LC_ALL=C

# If the environment's /tmp is too small
#export TMPDIR="$HOME"/tmp

#archome=
rechome=~/doc/fari-recipes
prefix=/usr/local

# nonempty strings mean yes, an empty string means no
install_strip=1
install_gzip=1

# User-defined variables; used in recipes
optflags=-O2
